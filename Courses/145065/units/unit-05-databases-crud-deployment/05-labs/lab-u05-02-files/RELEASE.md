# Release notes · Line 3 Maintenance Log

Riverside Fabrication is a composite: an invented shop with invented records.

TODO Lab U05-02, Part 4. Write this file before you tag anything:

1. A heading that names the release: the letter v, then the version number from app.py
2. One paragraph: why a release gets a name, and what this first deployed version is to every later change
3. What v1.0.0 contains, as a list
4. The three places the version is written, and that all three must agree
5. The commands to tag the release, and what an annotated tag records
6. A table with one row per version, starting with 1.0.0

The table must look like this, because `release_check.py` reads it:

| Version | Change | Why | Found by |
|---|---|---|---|
